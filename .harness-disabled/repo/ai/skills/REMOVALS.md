# ai/skills Removal & Disablement Ledger

One row per disabled entry, covering both `"off"` keys in `.claude/settings.json`'s `skillOverrides` block (110 today) and agent-file shims under `ai/agents/` that were superseded rather than gated by an override — the latter have no `skillOverrides` key by design, so a strict one-row-per-override reading under-counts them (117 rows total). States: `retired` (permanently disabled, no plan to re-enable), `superseded-by <skill>` (functionality now lives elsewhere), `disabled-pending <reason>` (off for a concrete, nameable reason, not permanently decided). Built-in skills (`review`, `simplify`, `code-review`, `run`, `explore`) are out of scope — they are not part of this repo's `ai/skills/` tree.

| skill | state | rationale |
|---|---|---|
| `ado-workitem` | disabled-pending | ai/skills/ado-workitem/ implements Azure DevOps work-item CLI helpers; off by default because it requires az CLI auth/an ADO organization context not configured in every environment. |
| `ai-usage-analyst` | disabled-pending | ai/skills/ai-usage-analyst/ is a fully built usage-dashboard analyst that depends on activtrak/usage data sources not available in every environment. **Contradiction found 2026-07-28 (Goal 05 audit): this row asserts disabled-pending, but `ai-usage-analyst` has no `"off"` key in `.claude/settings.json`'s `skillOverrides` at all, so nothing gates it and the skill is live.** Resolve one way or the other: either add the `"off"` override (matching this row's stated intent) or delete this row (accepting that the skill is enabled). Not decided here — adding an override would silently disable a live skill. |
| `aside` | disabled-pending | aside is a slash command (ai/commands/aside.md, "pause to answer a side question"), not an ai/skills/ entry; the skillOverrides key has no matching skill directory to gate. |
| `auc-dev-a` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-dev-b` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-dev-c` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-devintegration-suite` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-qa` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-sm` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `auc-tech-writer` | disabled-pending | Project-scoped agent for the downstream auc-conversion project (see plans/2026-04-02-bmad-learnings.md); lives in that project's .claude/agents/, not in this dotfiles repo's ai/skills/ tree — off here pending a decision on whether to mirror it. |
| `autoresearch` | disabled-pending | ai/skills/autoresearch/ is a fully built autonomous metric-optimization loop (predict/learn/fix phases); kept off by default pending a guardrail/trust review before letting it auto-invoke. |
| `azure-devops-cli` | disabled-pending | ai/skills/azure-devops-cli/ wraps `az repos` for ADO pull-request work; off because it needs an authenticated az CLI and an explicit `--organization`, which is not configured in every environment. **Conflict to resolve:** CLAUDE.md cites this skill as live guidance for ADO repos, so a documented workflow currently points at a disabled skill — either re-enable it or move that guidance out of CLAUDE.md. |
| `bmad-bmad-master.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmad-master.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-analyst.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-analyst.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-architect.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-architect.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-check-implementation-readiness` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-check-implementation-readiness directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-code-review` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-code-review directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-correct-course` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-correct-course directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-architecture` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-architecture directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-epics-and-stories` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-epics-and-stories directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-excalidraw-dataflow` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-excalidraw-dataflow directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-excalidraw-diagram` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-excalidraw-diagram directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-excalidraw-flowchart` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-excalidraw-flowchart directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-excalidraw-wireframe` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-excalidraw-wireframe directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-prd` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-prd directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-product-brief` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-product-brief directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-story` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-story directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-create-ux-design` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-create-ux-design directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-dev-story` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-dev-story directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-dev.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-dev.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-document-project` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-document-project directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-master` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-master directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-pm.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-pm.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-quick-dev` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-quick-dev directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-quick-flow-solo-dev.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-quick-flow-solo-dev.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-quick-spec` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-quick-spec directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-research` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-research directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-retrospective` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-retrospective directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-sm.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-sm.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-sprint-planning` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-sprint-planning directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-sprint-status` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-sprint-status directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-tea.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-tea.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-tech-writer-tech-writer.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-tech-writer-tech-writer.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-atdd` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-atdd directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-automate` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-automate directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-ci` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-ci directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-framework` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-framework directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-nfr` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-nfr directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-test-design` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-test-design directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-test-review` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-test-review directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-testarch-trace` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-testarch-trace directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-bmm-ux-designer.agent` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-bmm-ux-designer.agent directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-brainstorming` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-brainstorming directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-custom-pr-review` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Its adversarial pass is now lensed-review's `doubt` lens; only the Charcoal layer-walking plumbing survives, as a shim pinning the per-layer thread header and the Layer/Branch/PR#/Findings/Posted summary. Honest negative result: the earlier "superseded-by pr-review" call retired nothing — pr-review carried its own duplicate review logic, so two copies survived instead of one. |
| `bmad-editorial-review-prose` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-editorial-review-prose directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-editorial-review-structure` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-editorial-review-structure directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-help` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-help directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-index-docs` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-index-docs directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-party-mode` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-party-mode directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-review-adversarial-general` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-review-adversarial-general directory exists — off pending an adoption decision on the BMAD framework. |
| `bmad-shard-doc` | disabled-pending | BMAD framework artifact lives under .cursor/commands/bmad/** (agents/workflows/tasks), not ported into ai/skills/; no ai/skills/bmad-shard-doc directory exists — off pending an adoption decision on the BMAD framework. |
| `checkpoint` | superseded-by hyper-atomic-commits-reference | ai/skills/checkpoint/'s WIP-commit flow duplicates the canonical checkpoint.sh invocation now documented directly in ai/rules/hyper-atomic-commits.md and the hyper-atomic-commits-reference skill; the standalone checkpoint skill is redundant with that reference. |
| `ci` | superseded-by ci-watch | ai/skills/ci/ predates the fire-and-forget watcher; ci-watch (plus ci-status/ci-monitor) owns CI observation now, and `ci` was the strongest sub-threshold collision partner for ci-status in the Step 7 scan (0.437). Off to keep the routing space clean. |
| `claude-api` | disabled-pending | No ai/skills/ or ai/commands/ artifact exists for this key anywhere in the repo; it appears to be a reserved/placeholder override with no implementation built yet. |
| `claude-architect` | disabled-pending | ai/skills/claude-architect/ is a meta-skill for designing new Claude primitives (skills, hooks, agents); kept off by default so its broad primitive-decision triggers don't fire during normal work — invoked deliberately instead. |
| `claude-auto` | superseded-by auto-ship | ai/skills/claude-auto/'s 6-phase autonomous pipeline overlaps with ai/skills/auto-ship/, which now implements the git-lifecycle portion (commit -> push -> PR -> CI-wait -> ship -> sync -> clean) under D3 autonomy tiers, recently enabled per commit 7390f63; claude-auto's remaining GH-issue-trigger scope was not adopted. |
| `claude-code-review-agent` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Agent at .claude/agents/claude-code-review-agent.md, not an ai/skills/ entry. Hook-safety/POSIX/tool-usage review is covered by the `correctness` and `resilience` lenses; the agent file is now a forwarder pinning its severity-ranked findings report. |
| `claude-in-chrome` | superseded-by chrome-mcp-efficiency | ai/skills/chrome-mcp-efficiency/SKILL.md now owns guidance for mcp__claude-in-chrome__* tool calls, enforced via .claude/hooks/chrome-mcp-guard.sh; the claude-in-chrome key itself has no ai/skills directory of its own. |
| `code-health` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Complexity/length/nesting/duplication biomarkers are now the `style` lens. The shim keeps only the 1-10 scoring formula, score bands, hotspot mining, `--gate` exit-code behaviour, and refactor queue — its pinned output contract. |
| `context-eval` | disabled-pending | context-eval is a Claude-Code-client-local script/command (.claude/scripts/context-eval-report.sh, .claude/commands/context-eval.md per plans/2026-07-14-agentic-loop-optimization.md), not a shared ai/skills/ entry; off pending a decision on porting it in. |
| `continuous-learning` | disabled-pending | ai/skills/continuous-learning/ is a long-running learning-loop skill kept off pending the same autonomous-loop safety review noted for autoresearch, which it closely relates to. |
| `data-analyst` | disabled-pending | ai/skills/data-analyst/ depends on activtrak/OKR/cross-notebook data sources not available in every environment (same constraint as ai-usage-analyst, which it is described as a parent pattern for). |
| `database-reviewer` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Agent at .claude/agents/database-reviewer.md. Index/RLS/query-plan/N+1 auditing runs as the `correctness` and `performance` lenses scoped to schema and query files; the agent file is now a forwarder. Note `performance` currently ships disabled (empty `instruction`), so DB perf review must be requested explicitly until it is filled in — recorded here rather than silently dropped. |
| `evolve` | disabled-pending | Slash command at ai/commands/evolve.md; no ai/skills/evolve directory exists to gate. |
| `fewer-permission-prompts` | disabled-pending | No ai/skills/ or ai/commands/ artifact exists for this key; the theme (reducing permission prompts via autoAllowBashIfSandboxed / hard_deny) is discussed in plans/2026-06-12-ai-primitives-upgrade.md but no dedicated skill was ever built. |
| `findings` | disabled-pending | Slash command at ai/commands/findings.md; no ai/skills/findings directory exists to gate. |
| `fury` | disabled-pending | ai/skills/fury/ (QA/TDD agent) has a deliberately extremely broad auto-invoke trigger ("if there's even a 1% chance tests are relevant"); kept off pending trigger-scoping review to avoid firing on unrelated requests. |
| `hawk` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Its Architecture/Quality/Resilience/Security passes map to the `correctness`, `style`, `resilience`, and `security` lenses; `--adversarial` maps to the `doubt` lens. The shim pins the severity-ranked findings table, the deterministic verdict ladder, and `--post-pr` inline/block comment behaviour. |
| `hookify` | disabled-pending | Slash command at ai/commands/hookify.md; no ai/skills/hookify directory exists to gate. |
| `hookify-list` | disabled-pending | Slash command at ai/commands/hookify-list.md; no ai/skills/hookify-list directory exists to gate. |
| `hyper-commit-setup` | disabled-pending | ai/skills/hyper-commit-setup/SKILL.md itself declares `disable-model-invocation: true` in its frontmatter — it is a manual one-time bootstrap skill, not meant for model auto-invoke, matching its off state here. |
| `ia` | disabled-pending | Slash command at ai/commands/ia.md; no ai/skills/ia directory exists to gate. |
| `init` | disabled-pending | No ai/skills/init directory exists; "init" collides with Claude Code's built-in /init command (CLAUDE.md generation) — off to avoid shadowing the built-in until/unless a distinct skill is designed under a different name. |
| `instinct-export` | disabled-pending | Slash command at ai/commands/instinct-export.md; no ai/skills/instinct-export directory exists to gate. |
| `ironman` | disabled-pending | ai/skills/ironman/ (code-implementation agent) has very broad auto-invoke triggers ("implement", "fix this bug", "make it work"); kept off pending the same trigger-scoping review as fury/stark to avoid collisions with default Claude Code behavior. |
| `keybindings-help` | disabled-pending | Referenced only as a planned companion to tmux-automation in plans/melodic-wiggling-cloud.md ("Builds on: tmux-automation, keybindings-help"); no ai/skills/keybindings-help directory was ever created. |
| `lit-ingest` | disabled-pending | ai/skills/lit-ingest/ is a literature/reference ingestion tool meant for occasional manual invocation, not general auto-invoke; off by default. |
| `loop` | disabled-pending | Referenced as "/loop" for recurring LLM work in ai/skills/bg-job/SKILL.md, but no ai/skills/loop directory exists in this repo — the recurring-work pattern is currently implemented via CronCreate/RemoteTrigger directly, not a standalone loop skill. |
| `monitor-patterns` | disabled-pending | Explicitly documented as disabled in ai/rules/agent-user-global.md's Background Monitoring section ("this skill is currently disabled via skillOverrides ... read ai/skills/monitor-patterns/SKILL.md directly"); intentional, not a placeholder. |
| `ollama` | disabled-pending | ai/skills/ollama/ routes work to a local Ollama model; off by default because it requires a local Ollama installation not present in every environment. |
| `pctx-code-mode` | retired | Directory deleted 2026-08-03 with the pctx gateway itself. The skill documented `execute_typescript` code mode, which has no equivalent now that Serena, LeanCtx, Repomix and Graphify are direct MCP servers; independent calls are issued as parallel tool calls instead. |
| `performance-optimizer` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Agent at .claude/agents/performance-optimizer.md. Its scope is the `performance` lens, which ships with an empty `instruction` (disabled) — an honest negative: the consolidated skill does not yet reproduce this agent's profiling depth, and the lens is intentionally opt-in rather than pretending parity. |
| `pr-review` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Its multi-agent set collapses into the lens set. The shim keeps forge detection (GitHub/Azure DevOps), Charcoal stack layering, and PR posting, and pins the `## PR Review` report header, dedupe/filter rules, and CLEAN/REVIEW NEEDED/BLOCKING verdict line. |
| `qmd-routing` | superseded-by tool-routing | ai/skills/qmd-routing/ covered Qmd-vs-other-tool selection; that decision table now lives in ai/skills/tool-routing/SKILL.md, which is the single routing authority referenced from ai/rules/tool-priority.md §4. |
| `quarantine-analyst` | disabled-pending | ai/skills/quarantine-analyst/ is a fully built AUC-migration SQL Server quarantine-data analyst; off by default because it requires AUC project DB access (sqlcmd/dbt) not present outside that project context. |
| `quarantine-triage-live` | disabled-pending | ai/skills/quarantine-triage-live/ complements quarantine-analyst for live AUC migration incident triage; same AUC-project-scoped DB-access constraint keeps it off by default. |
| `release-prep` | disabled-pending | ai/skills/release-prep/ assumes a versioned-release workflow; this repo ships continuously via the stack/PR pipeline and cuts no releases, so there is nothing for it to drive. Off until a real release process exists. |
| `repomix` | superseded-by tool-routing | ai/skills/repomix/ described when to pack a codebase; Repomix is a direct MCP server and its selection rule (5+ files) lives in ai/skills/tool-routing/SKILL.md. Off to avoid a second copy of the same routing rule. |
| `resource-guard` | disabled-pending | Slash command at ai/commands/resource-guard.md; no ai/skills/resource-guard directory exists to gate. |
| `resume-context` | superseded-by session-artifacts | ai/skills/resume-context/ restored session state ad hoc; plans/active-context.md plus the session-artifacts skill are the durable mechanism, and ai/rules/context-and-compaction.md makes them the resume path. Off as a duplicate. |
| `security-reviewer` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Agent at .claude/agents/security-reviewer.md. OWASP endpoint/auth/validation/secrets/CVE checks are the `security` lens; the agent file is now a forwarder. |
| `schedule` | disabled-pending | Referenced as "/schedule" for remote-agent scheduling in ai/skills/ai-usage-analyst/SKILL.md and ai/skills/data-analyst/SKILL.md, but no ai/skills/schedule directory exists — the pattern is documented, not yet implemented as its own skill. |
| `session-defer` | disabled-pending | No ai/skills/session-defer directory exists in this repo; this key names a planned session-lifecycle command in the session continuity/session-artifacts design space that was never built as a standalone skill. |
| `session-done` | disabled-pending | No ai/skills/session-done directory exists in this repo; this key names a planned session-lifecycle command in the session continuity/session-artifacts design space that was never built as a standalone skill. |
| `session-next` | disabled-pending | No ai/skills/session-next directory exists in this repo; this key names a planned session-lifecycle command in the session continuity/session-artifacts design space that was never built as a standalone skill. |
| `session-picker` | disabled-pending | No ai/skills/session-picker directory exists in this repo; this key names a planned session-lifecycle command in the session continuity/session-artifacts design space that was never built as a standalone skill. |
| `session-undefer` | disabled-pending | No ai/skills/session-undefer directory exists in this repo; this key names a planned session-lifecycle command in the session continuity/session-artifacts design space that was never built as a standalone skill. |
| `silent-failure-hunter` | superseded-by lensed-review | Step 10 (2026-07-28) consolidation into ai/skills/lensed-review/. Agent at .claude/agents/silent-failure-hunter.md. Swallowed errors, bad fallbacks, and missing error propagation are covered by the `resilience` lens; the agent file is now a forwarder. |
| `squash-wip` | superseded-by hyper-atomic-commits-reference | ai/skills/squash-wip/ squashed WIP commits; the hyper-atomic workflow uses scripts/ai/checkpoint.sh for WIP and scripts/ai/commit.sh for real commits, so squashing after the fact is not part of the flow. Off to keep one commit discipline. |
| `stark` | disabled-pending | ai/skills/stark/ (architect/planner agent) shares the broad-trigger concern noted for fury/ironman; its own SKILL.md discusses deprecated-version transition planning, but the skill itself is not deprecated — it is off pending trigger-scoping review. |
| `start-story` | disabled-pending | ai/skills/start-story/ is a BMAD/AUC-style story-kickoff skill (branch naming like "1.9.feat-chunked-processing"); scoped to story-driven project workflows, not the default dotfiles flow — off pending adoption decision. |
| `story-complete` | disabled-pending | No ai/skills/story-complete directory exists; paired conceptually with start-story (which does exist and is itself disabled-pending) but the completion half was never built. |
| `tech-lead` | retired | Step 8 decision (2026-07-28, recorded in plans/2026-07-27-native-agent-orchestration.md Appendix B): skill stays disabled at both settings scopes, no spec-handoff/acceptance-gate retrofit. |
| `tmux-automation` | disabled-pending | ai/skills/tmux-automation/ (pane targeting, nvim-adjacent open, worktree popups) is distinct in scope from ai/skills/tmux-orchestrator/ (cross-CLI persistent orchestration); kept off pending a review of trigger overlap between the two before enabling both. |
| `update-config` | disabled-pending | Listed only in ai/skills/claude-architect-workspace's claude-architect SKILL.md "Related Skills" as "update-config: Configure settings.json (hooks, permissions, MCP)" — an aspirational related-skill reference, not an actual ai/skills/update-config directory. |
| `vision` | disabled-pending | ai/skills/vision/ (DevOps/CI/CD architect) has broad triggers that overlap with the already-enabled ci-watch/ci-monitor/ci-status skills; off pending scope narrowing to avoid duplicate invocation. |
| `watchdog-remediate` | disabled-pending | decisions/0007-migration-watchdog-single-skill.md designates watchdog-remediate as "the designated active-remediation skill" for migration-watchdog findings, but no ai/skills/watchdog-remediate directory exists in this repo yet — off pending that build-out. |
