Run the local context-evaluation report for this worktree.

## Instructions

1. Run:
   ```bash
   bash .claude/scripts/context-eval-report.sh
   ```
2. Return the report output directly.
3. Then give a brief interpretation:
   - If both `baseline` and `context-mode` are present, call out whether the automated signal currently favors the trial.
   - If only one mode is present, state what is still missing for a clean comparison.
4. Keep the response concise.
