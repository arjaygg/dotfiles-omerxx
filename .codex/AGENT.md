# Codex Compatibility Note

This file is intentionally thin.

- User-global Codex defaults are loaded from `.codex/config.toml` via `model_instructions_file`.
- Project-specific guidance for this repository lives in `AGENTS.md`.
- Tool-specific runtime and MCP behavior lives in `.codex/config.toml`.
