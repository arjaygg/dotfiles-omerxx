{
  "$schema": "https://json.schemastore.org/mcp.json",
  "mcpServers": {
    "pctx": {
      "command": "pctx",
      "args": [
        "mcp",
        "start",
        "--stdio",
        "-c",
        "${PCTX_CONFIG}"
      ]
    }
  }
}
