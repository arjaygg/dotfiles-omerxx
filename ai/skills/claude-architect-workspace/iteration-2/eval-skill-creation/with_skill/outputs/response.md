# claude-architect response

**Primitive used:** `skill` — a new reusable skill definition is the right primitive because this is a
repeatable, domain-specific audit workflow that should be invokable across any project containing
Kubernetes manifests.

---

## SKILL.md written

Path: `~/.dotfiles/ai/skills/k8s-security-audit/SKILL.md`

(For this eval, written to the outputs directory.)

---

## Symlink command

```bash
ln -sf ~/.dotfiles/ai/skills/k8s-security-audit ~/.claude/skills/k8s-security-audit
```

---

## Skill registered

`k8s-security-audit` skill created at `~/.dotfiles/ai/skills/k8s-security-audit/SKILL.md` and
symlinked to `~/.claude/skills/k8s-security-audit`. Invoke with `/k8s-security-audit` or via
the Skill tool with `skill: "k8s-security-audit"`.
