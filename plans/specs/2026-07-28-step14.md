---
status: draft
retry_count: 0
doubt_cycle_iteration: 0
review_loop_iteration: 0
followup_review_recommended: false
---

# Frozen Spec — Goal 05 Step 14: Orchestrator skeleton, interactive only

Branch/worktree label: `native-agent-orchestration-step14`
Base: `main` (currently `ae8876e`). Steps 1–13 and 16 are all merged; nothing is outstanding.

<intent-contract>
Produce `.claude/workflows/orchestrate.js` — the first file in that directory — as an
**interactive-only** orchestrator skeleton. It composes the pieces Steps 1–13 already landed:
it delegates review to the `lensed-review` skill rather than embedding review logic, and reads
the standing Definition of Done at the acceptance stage. It must survive this repo's own
Workflow guard: `pre-tool-gate-v2.sh` SECTION 8 hard-denies any script with more than 3 literal
`agent(` call sites. Verification is a dry run — `Workflow({scriptPath, args:{dryRun:true}})`
returning a schema-valid result — not a live multi-agent run. Unattended paths are Step 15's
scope and must not be built here.
</intent-contract>

## Task

Write the orchestrator skeleton. Everything it needs already exists on `main`:

| Dependency | Where it landed | How Step 14 uses it |
|---|---|---|
| `lensed-review` skill | Step 10 | the reviewer stage invokes it; no review logic inline |
| Definition of Done | Step 2, `ai/references/definition-of-done.md` (symlinked `.claude/references/`) | the acceptance stage reads it, or logs its absence |
| finding contract | Step 10, `ai/skills/cap/references/schemas.md` | the shape reviewer findings come back in |
| spec path convention | Step 3, `plans/specs/<label>.md` | how a worker stage receives its instructions |
| skill router / manifest | Step 12, `ai/skills/manifest.csv` | phase vocabulary the stages should align to |

Read **only** these sections of `plans/2026-07-27-native-agent-orchestration.md` — do not open
the whole 1034-line file, which has killed two prior sessions:

- **§3** (lines 73–85) — the ≤3 `agent(` hard deny, and "log every cap."
- **§21** (lines 422–455) — reviewer input isolation: ARTIFACT + CONTRACT only, never the claim,
  never your reasoning, never another reviewer's findings.
- **§13** (lines 258–277) — why interactive and unattended differ, so the boundary with Step 15
  is clear.
- **Step 14's own entry** (lines 883–892).

The Workflow tool's full authoring contract (`meta` literal rules, `agent()`/`parallel()`/
`pipeline()` semantics, `schema`, `label`, null-on-skip) is in the Workflow tool description
itself — consult that rather than guessing.

## Files

- `.claude/workflows/orchestrate.js` — new, the only production file in scope.
- A test is not named in the plan's Files list, but the dry-run check has to live somewhere
  runnable. Follow repo convention: `scripts/test_orchestrate_workflow.py`, `unittest`, matching
  the 27 existing `scripts/test_*.py`. Flag this as a deviation in the PR body.

Touch nothing else. In particular do not amend `schemas.md` — Step 15 owns the next change there.

## Acceptance

Verbatim from the plan, decomposed:

- [ ] Required `export const meta = {name, description, phases}` present as a **pure literal** —
      no variables, calls, spreads, or interpolation.
- [ ] `grep -c 'agent(' .claude/workflows/orchestrate.js` ≤ 3. Show the number.
- [ ] Every `agent()` call passes both `schema` and `label`.
- [ ] Every `agent()` result is null-guarded (a skipped or dead agent returns `null`).
- [ ] The reviewer stage invokes `lensed-review` and passes **artifact + contract only** (§21) —
      not the claim, not prior reasoning, not another reviewer's findings.
- [ ] The acceptance stage reads `ai/references/definition-of-done.md`, or logs its absence when
      the file is missing. Both paths exercised.
- [ ] `args.dryRun` stubs `agent()` with schema-shaped fixtures, and
      `Workflow({scriptPath, args:{dryRun:true}})` completes with a schema-valid result.
- [ ] Any bound on coverage (top-N, sampling, no-retry) is `log()`ed — silent truncation is the
      §3 failure this exists to prevent.
- [ ] No unattended-mode code: no `run_in_background`, no detached paths, no HALT protocol.
      Those are Step 15.

## Constraints

- **Interactive only.** If a stage seems to need an unattended path, leave a comment naming
  Step 15 and move on. Do not build it.
- **Anti-nesting:** implement directly. Do not spawn subagents — the session's standing
  instruction forbids the Agent tool unless the user asks for it, and a live multi-agent run is
  not required by the acceptance criteria. The dry run is the check.
- Branch `chore/native-agent-orchestration-step14` in its own worktree under `.trees/`.
  Never commit to `main`; `protect-main.sh` blocks it anyway.
- Commit via `~/.dotfiles/scripts/ai/commit.sh` (conventional format, meaningful body).
  Raw `git commit` is hook-blocked. `atomic-status.sh` is blocked by the lean-ctx shell
  allowlist in this session type — check thresholds with `git status --short` instead
  (≤7 files, ≤3 subsystems, ≤300 diff lines).
- Native `Edit`/`Grep`/`Glob` are denied here. Use `ctx_shell`/`ctx_read`/`ctx_search`, and the
  `Write` tool for whole files. To rewrite an existing file, `rm` it first — `Write` refuses to
  overwrite a file this session has not read.
- `python3 -c` and interpreter heredocs are blocked. Write a script to the scratchpad and run it.
- Before finishing, run all three gates and paste their output:
  `python3 scripts/validate_skills.py`, `python3 scripts/run_evals.py --summary`,
  `python3 -m unittest` over the new test.

## Spec Change Log

(none yet)

## Review Triage Log

(none yet)
