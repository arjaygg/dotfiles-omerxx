{
  "name": "vector_search",
  "description": "Search by meaning. Finds relevant documents even when they use different words than the query — handles synonyms, paraphrases, and related concepts.",
  "arguments": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Natural language query - describe what you're looking for"
      },
      "limit": {
        "default": 10,
        "description": "Maximum number of results (default: 10)",
        "type": "number"
      },
      "minScore": {
        "default": 0.3,
        "description": "Minimum relevance score 0-1 (default: 0.3)",
        "type": "number"
      },
      "collection": {
        "description": "Filter to a specific collection by name",
        "type": "string"
      }
    },
    "required": [
      "query"
    ],
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}