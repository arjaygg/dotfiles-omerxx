{
  "name": "get_function_details",
  "description": "Get detailed TypeScript definitions for specific functions.\n\nProvide function names in 'namespace.functionName' format (e.g., 'Notion.apiPostSearch').\nReturns full type signatures, parameter schemas, and return types.\n\nNote: Functions showing Promise<any> return type have no schema - the actual runtime\nvalue is a parsed JavaScript object (never a JSON string).",
  "arguments": {
    "type": "object",
    "properties": {
      "functions": {
        "description": "List of functions to get details of.",
        "type": "array",
        "items": {
          "$ref": "#/$defs/FunctionId"
        }
      }
    },
    "required": [
      "functions"
    ],
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "GetFunctionDetailsInput",
    "$defs": {
      "FunctionId": {
        "description": "Function representation in the form should be in the form '<namespace>.<function name>'. e.g. If there is a function `getData` within the `DataApi` namespace the value provided in this field is DataApi.getData",
        "type": "string"
      }
    }
  }
}