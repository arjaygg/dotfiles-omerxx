# Cursor runtime state — lives in the real ~/.cursor/, not in dotfiles.
# Everything else in this directory is tracked config.
plans/
projects/
plugins/
skills-cursor/
ai-tracking/
browser-logs/
extensions/
managed/
worktrees/
subagents/
agent-cli-state.json
argv.json
prompt_history.json
unified_repo_list.json
ide_state.json
mcp.json.bak
# Cursor project-level config (created by Cursor for the dotfiles dir itself)
.cursor/
# Accidental recursive Library symlink
Library/Library
