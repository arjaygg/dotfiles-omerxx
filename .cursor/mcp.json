{
  "mcpServers": {
    "pctx": {
      "type": "stdio",
      "command": "pctx",
      "args": [
        "mcp",
        "start",
        "--stdio",
        "-c",
        "${env:HOME}/.config/pctx/pctx.json"
      ]
    }
  }
}
