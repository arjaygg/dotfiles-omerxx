# Goals Index

| Goal | File | Status | Notes |
| --- | --- | --- | --- |
| 01 | [Evaluate and optimize AI agent harness](2026-07-14-01-agentic-loop-optimization.md) | Completed (bounded Codex slice) | Live rewrite intentionally skipped because the semantic delta was zero. |
| 02 | [Cross-client config portability and residual-gap cleanup](2026-07-15-02-cross-client-config-portability.md) | Completed (bounded slice) | User approved "all 3 clients, read-only first" scope. Steps 1-6, 8, 9 all done (base templates, manifest, tests, README, Gate-1 compare for gemini/cursor/windsurf; security-regression fix; START_HERE memory). Step 7 (live write) intentionally out of scope. See `plans/2026-07-16-cross-client-config-portability.md`. |
| 03 | [Deterministically enforce model-tier and subagent-fan-out routing](2026-07-26-03-deterministic-model-routing-enforcement.md) | Completed (warn-only) | All 8 steps done: opusplan/sonnet drift resolved (ADL-020), agent `model:` frontmatter hard-enforced in `config-integrity.sh` (ADL-021), `Workflow` fan-out + `Agent` tier-mismatch gates added to `pre-tool-gate-v2.sh` in warn-only mode with recorded fire/no-fire evidence (ADL-022), base-template drift caught and fixed (ADL-023). Main-loop tier selection remains structurally advisory-only (no hook can call `/model`) — documented in `model-routing/SKILL.md`'s new Enforcement section. Promotion of either new gate to `deny` is a separate future decision, not part of this goal. |
| 04 | [Agentic Git Lifecycle Pipeline](2026-07-25-03-agentic-git-pipeline.md) | Done, with documented gaps | Uses the plan's Step 0-7 numbering; file/goal-doc retain their original "03" numeral (this branch was authored in parallel with goal 03 above, which claimed the number first on `main` — renumbered here to 04 to avoid collision, filename left unchanged since it's referenced throughout this goal's own commit history). Steps 0-6 fully committed. Step 7 (end-to-end shakedown) done with one documented acceptance-criteria gap: the CI-wait and sync-against-main legs were not exercised because the shakedown PR's base was an intermediate branch, not `main` (see the goal doc's Step 7 notes and ADL-021 there). See `plans/2026-07-25-agentic-git-pipeline.md` for the full D1-D7 decision record. |
| 05 | [Native Agent Orchestration Harness](2026-07-28-05-native-agent-orchestration-harness.md) | Completed (audited) | All 18 steps done and both cross-cutting criteria met; every acceptance box carries the evidence for its check. Design: `plans/2026-07-27-native-agent-orchestration.md`. Steps 1-17 shipped as PRs #361-#392 (32 merged); Step 18 (autonomy ladder) plus the closeout audit are PR #395. **The final audit re-ran all 54 criteria and did not rubber-stamp them:** 41 PASS, 8 FAIL, 5 UNCLEAR. Fixed: a lint baseline whose dead exemptions silently disabled Step 1's own anti-nesting invariant for four agents; `orchestrate.js` writing no terminal status on a thrown stage while its comment claimed a guarantee, and a SIGKILL detector with no producer; 6 dead hook matcher keys; 7 unledgered disabled skills; committed `</replace>` artifacts in two SKILL.md files. Five criteria were unsatisfiable as worded (a `grep` count that matched its own prohibition, "fewer than" an already-zero baseline, an undefined "discipline skill" population) and were amended in the plan rather than passed vacuously. **Open follow-ups, none blocking:** Step 19 (pre-action A2 enforcement — a Stop hook cannot precede the merge it guards); `evals/cases/auto-ship.json` as the C6 risk-acceptance discharge before its 2026-10-31 expiry; `orchestrate.js` builds undated `plans/specs/<label>.md` while the hook enforces a date, so a real halt write would be blocked; `azure-devops-cli` is disabled while `CLAUDE.md` cites it as live guidance. |
